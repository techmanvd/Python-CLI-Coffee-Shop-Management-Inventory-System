# Coffee Shop Management System ☕

A Python-based Command Line Interface (CLI) application that simulates a coffee shop's Point of Sale (POS) and inventory system. This project uses Object-Oriented Programming (OOP) to handle orders, track sales, and manage stock levels in real-time.

## 🚀 Features

* **Smart Ordering System:** * Checks inventory before confirming an order.
    * Calculates total cost based on quantity.
    * Includes an order confirmation step (Yes/No).
* **Real-time Inventory Tracking:**
    * Automatically deducts stock when an order is finalized.
    * Prevents sales if an item is out of stock.
* **Sales Reporting:** Tracks total revenue generated during the session.
* **Interactive Menu:** Clean, looped interface for continuous operation.

## 🛠️ Technologies Used

* **Language:** Python 3.x
* **Concepts:** Classes & Objects, Dictionaries (Key-Value pairs), Conditional Logic, Input Handling.

## 📋 Prerequisites

To run this application, you need **Python** installed on your computer.

Check your installation by running:
```bash
python --version
